# Development-only host access. Do not use this override on the public VPS.
services:
  db:
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
  backend:
    ports:
      - "127.0.0.1:${BACKEND_PORT:-8000}:8000"
  frontend:
    ports:
      - "127.0.0.1:${APP_PORT:-3000}:3000"
