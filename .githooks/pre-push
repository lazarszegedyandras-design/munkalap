#!/bin/sh
while read local_ref local_sha remote_ref remote_sha; do
  case "$remote_ref" in
    refs/heads/main|refs/heads/master)
      echo "Direct pushes to $remote_ref are disabled. Use a reviewed merge." >&2
      exit 1
      ;;
  esac
done
exit 0
