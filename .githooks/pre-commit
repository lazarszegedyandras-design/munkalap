#!/bin/sh
branch=$(git symbolic-ref --quiet --short HEAD) || exit 0
case "$branch" in
  main|master)
    echo "Direct commits on $branch are disabled. Use a task branch." >&2
    exit 1
    ;;
esac
exit 0
